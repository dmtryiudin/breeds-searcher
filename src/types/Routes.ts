export enum Routes {
  MAIN = "/",
  BREED_ITEM = "/breed",
}

export enum ApiRoutes {
  GET_BREEDS = "/breeds",
  SEARCH_BREEDS = "/breeds/search",
  IMAGES = "/images/search",
}

export enum InnerApiRoutes {
  DEFAULT_BREEDS = "/default-breeds",
  SEARCH_BREEDS = "/search-breeds",
  BREED_ITEM = "/breed-item",
}
